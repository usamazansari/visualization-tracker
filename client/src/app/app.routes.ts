export enum APP_ROUTES {
  HOME = 'home',
  DEDUPLICATION = 'dedup',
  TESTSUITE = 'test-suite',
  PERCEPTUAL_HASH = 'perceptual',
  IMAGE_ANALYTCS = 'image',
  ERROR = "error",
  WILDCARD = "**",
  EMPTY = ''
}
